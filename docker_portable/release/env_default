# .env_default

# Replace with a secure random string in production
SECRET_KEY=YOUR_SECRET_KEY

# Database credentials (default dev values)
DB_USER=finance_user
DB_PASSWORD=YOUR_DB_PASSWORD
DB_PORT=3306
DB_NAME=finance_tracker

# Flask configuration (PortableConfig for container setups)
FLASK_CONFIG=config.config.PortableConfig
